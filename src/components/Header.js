import React from 'react';
import HeaderSearch from './HeaderSearch';
import './Header.css';

const Header = (props) => {
  return (
    <header>
      <HeaderSearch />
    </header>
  );
}

export default Header;